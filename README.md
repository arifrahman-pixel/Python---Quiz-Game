# Console Quiz Game

A simple, interactive multiple-choice quiz program written in Python. This project demonstrates core Object-Oriented Programming (OOP) concepts, including classes, constructors (`__init__`), and object instantiation.

## How to Run
Run the main script using Python:
```bash
python main.py